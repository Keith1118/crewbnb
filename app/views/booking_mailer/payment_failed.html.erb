<h1>We Couldn't Take Payment</h1>

<p>Hi <%= @guest.first_name || @guest.email.split("@").first %>,</p>

<p>Your saved card was declined when we tried to take payment for <strong><%= @property.title %></strong>. This is usually an expired card, insufficient funds, or your bank asking for extra confirmation.</p>

<p><strong>Your dates are still held</strong>, but only until <%= @booking.payment_due_by&.strftime("%A %d %B at %H:%M") %>. After that the booking is cancelled and the dates go back on sale.</p>

<div class="detail-box">
  <table width="100%" cellpadding="0" cellspacing="0" style="font-size: 14px;">
    <tr>
      <td style="padding: 6px 0; color: #6b7280;">Check-in</td>
      <td style="padding: 6px 0; color: #111c2d; font-weight: 600; text-align: right;"><%= @booking.check_in.strftime("%A, %d %B %Y") %></td>
    </tr>
    <tr style="border-top: 1px solid #e5e7eb;">
      <td style="padding: 10px 0 6px; color: #111c2d; font-weight: 700;">Amount due</td>
      <td style="padding: 10px 0 6px; color: #111c2d; font-weight: 700; text-align: right; font-size: 16px;">&euro;<%= number_with_precision(@booking.total_price, precision: 2, delimiter: ",") %></td>
    </tr>
  </table>
</div>

<p style="text-align: center; margin: 24px 0;">
  <a href="<%= new_booking_payment_url(@booking) %>" class="btn">Pay Now</a>
</p>

<p>Paying takes a minute and secures your stay.</p>
