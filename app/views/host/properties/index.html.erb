<% content_for(:page_title, "My Properties") %>
<% content_for(:title, "My Properties - Crewbase Host") %>

<div class="space-y-6">
  <%# Header %>
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
    <div>
      <h2 class="font-headline text-2xl font-extrabold text-ink tracking-tight">My Properties</h2>
      <p class="text-sm text-muted mt-1"><%= @properties.count %> total properties</p>
    </div>
    <%= link_to new_host_property_path, class: "inline-flex items-center gap-2 bg-brand text-white px-5 py-2.5 rounded-xl font-bold text-sm active:scale-95 transition-transform" do %>
      <span class="material-symbols-outlined text-lg">add</span>
      New Property
    <% end %>
  </div>

  <% if @properties.any? %>
    <%# Properties Table %>
    <div class="bg-white rounded-2xl border border-line overflow-hidden">
      <div class="overflow-x-auto">
        <table class="w-full">
          <thead>
            <tr class="border-b border-line">
              <th class="text-left px-6 py-4 text-xs font-bold text-muted uppercase tracking-wider">Property</th>
              <th class="text-left px-6 py-4 text-xs font-bold text-muted uppercase tracking-wider hidden sm:table-cell">City</th>
              <th class="text-left px-6 py-4 text-xs font-bold text-muted uppercase tracking-wider hidden md:table-cell">Status</th>
              <th class="text-left px-6 py-4 text-xs font-bold text-muted uppercase tracking-wider hidden md:table-cell">Price</th>
              <th class="text-left px-6 py-4 text-xs font-bold text-muted uppercase tracking-wider hidden lg:table-cell">Bookings</th>
              <th class="text-right px-6 py-4 text-xs font-bold text-muted uppercase tracking-wider">Actions</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-line">
            <% @properties.each do |property| %>
              <% hue_1 = (property.id * 37) % 360 %>
              <% hue_2 = (hue_1 + 40) % 360 %>
              <tr class="hover:bg-neutral-100 transition-colors">
                <%# Property Name + Thumbnail %>
                <td class="px-6 py-4">
                  <div class="flex items-center gap-3">
                    <div class="w-12 h-12 rounded-xl overflow-hidden flex-shrink-0">
                      <% if property.images.attached? %>
                        <%= image_tag sized_image(property.images.first, resize_to_fill: [96, 96]), class: "w-full h-full object-cover" %>
                      <% else %>
                        <div class="w-full h-full" style="background: linear-gradient(135deg, hsl(<%= hue_1 %>, 45%, 35%) 0%, hsl(<%= hue_2 %>, 55%, 25%) 100%);"></div>
                      <% end %>
                    </div>
                    <div>
                      <p class="text-sm font-semibold text-ink truncate max-w-[200px]"><%= property.title %></p>
                      <p class="text-xs text-muted sm:hidden"><%= property.city %></p>
                    </div>
                  </div>
                </td>

                <%# City %>
                <td class="px-6 py-4 hidden sm:table-cell">
                  <span class="text-sm text-muted"><%= property.city %></span>
                </td>

                <%# Status Badge %>
                <td class="px-6 py-4 hidden md:table-cell">
                  <% status_styles = {
                    "draft" => "bg-neutral-100 text-muted",
                    "published" => "bg-brand/15 text-brand",
                    "archived" => "bg-error-container/20 text-error"
                  } %>
                  <span class="inline-flex items-center px-2.5 py-1 rounded-lg text-xs font-bold uppercase tracking-wider <%= status_styles[property.status] || status_styles['draft'] %>">
                    <%= property.status %>
                  </span>
                </td>

                <%# Price %>
                <td class="px-6 py-4 hidden md:table-cell">
                  <span class="text-sm font-semibold text-ink"><%= number_to_currency(property.price_per_night) %></span>
                  <span class="text-xs text-muted">/night</span>
                </td>

                <%# Bookings Count %>
                <td class="px-6 py-4 hidden lg:table-cell">
                  <span class="text-sm text-muted"><%= property.bookings.count %></span>
                </td>

                <%# Actions %>
                <td class="px-6 py-4 text-right">
                  <div class="flex items-center justify-end gap-2">
                    <%= link_to host_calendar_path(property_id: property.id), class: "p-2 rounded-lg hover:bg-neutral-100 transition-colors text-muted hover:text-ink", title: "Calendar" do %>
                      <span class="material-symbols-outlined text-lg">calendar_month</span>
                    <% end %>
                    <%= link_to host_property_path(property), class: "p-2 rounded-lg hover:bg-neutral-100 transition-colors text-muted hover:text-ink", title: "View" do %>
                      <span class="material-symbols-outlined text-lg">visibility</span>
                    <% end %>
                    <%= link_to edit_host_property_path(property), class: "p-2 rounded-lg hover:bg-neutral-100 transition-colors text-muted hover:text-ink", title: "Edit" do %>
                      <span class="material-symbols-outlined text-lg">edit</span>
                    <% end %>
                    <%= link_to property_path(property), target: "_blank", class: "p-2 rounded-lg hover:bg-neutral-100 transition-colors text-muted hover:text-ink", title: "View public listing" do %>
                      <span class="material-symbols-outlined text-lg">open_in_new</span>
                    <% end %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>

    <%# Pagination %>
    <% if @pagy.pages > 1 %>
      <div class="flex justify-center mt-6">
        <nav class="inline-flex items-center gap-1 text-sm font-medium">
          <%== pagy_nav(@pagy) %>
        </nav>
      </div>
    <% end %>
  <% else %>
    <%# Empty State %>
    <div class="text-center py-20 bg-white rounded-2xl border border-line">
      <span class="material-symbols-outlined text-6xl text-neutral-300">real_estate_agent</span>
      <h3 class="font-headline text-lg font-bold text-ink mt-4">No properties yet</h3>
      <p class="text-muted mt-2 max-w-md mx-auto">
        Get started by listing your first work-ready property.
      </p>
      <%= link_to new_host_property_path, class: "inline-flex items-center gap-2 mt-6 bg-brand text-white px-6 py-3 rounded-xl font-bold text-sm active:scale-95 transition-transform" do %>
        <span class="material-symbols-outlined text-lg">add</span>
        List Your First Property
      <% end %>
    </div>
  <% end %>
</div>
